#include "main.h"

void _puts(char *str)

#include <stdio.h>

/**
 * _puts - function that takes a string
 *
 * main - Entry point
 *
 * Return: Always 0 (Success)
 */

int main(void)
{
	 _puts("\"Programming is like building");

         return (0);
}
