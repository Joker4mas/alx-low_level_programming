#include <stdio.h>

/**
 * _puts - function that takes a string
 *
 * main - Entry point
 *
 * Return: Always 0 (Success)
 */

int  _puts(char *str)
{
	 _puts("\"Programming is like building");
         return (0);
}
