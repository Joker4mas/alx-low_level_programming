#include  "main.h"
#include <stdio.h>

/**
 * main - Entry point
 * @argc : argument count
 * @argv : argument vector
 * Reeturn : Awlays 0 (success)
 */

int main(int argc_attribute_((unsed)), char **argv)
{
	printf("%s\n", argv);
	
	return (0);
}
