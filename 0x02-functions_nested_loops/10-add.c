#include "main.h"

/**
 * add - function that adds two numbers.
 *
 * @x: is a parameter used.
 *
 * @y: is a parameter used.
 *
 * Return: Always 0 (success).
 */

int add(int x, int y)
{
	return (x + y);
}
