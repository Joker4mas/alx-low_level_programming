#include "main.h"
#include <string.h>


/**
 * _strlen_recursion - function that gives the lenght
 * of a string
 * @s: string to be used
 * Return: Always 0 (success)
 */

int _strlen_recursion(char *s)
{
	return (strlen(s));
}
