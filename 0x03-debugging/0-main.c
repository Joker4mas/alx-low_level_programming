#include "main.h"

/**
 * main - Entry point 
 * 
 * function to test positive or negative integes
 *
 * Return: Always 0 (success).
 */

int main(void)
{
	int i;

	n = i;
	positive_or_negative(i);

	return (0);
}
