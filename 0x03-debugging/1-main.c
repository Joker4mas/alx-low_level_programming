#include "stdio.h"

/**
 * main  - a function to test the print integer if it's -ve 
 * or -ve
 * Return: Always 0 (success)
 */

int main(void)
{
	int n;
	printf("incoming infinite loop:(\n");
	n = 0;


	printf("infinite loop avoided \\o/\n");
	return (0);
}
